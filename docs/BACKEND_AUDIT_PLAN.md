# План проверки backend CafeHelp

**Дата актуализации:** 30 июля 2026 года  
**Статус:** подготовлен план проверки; исправления в backend в рамках этого этапа не вносились  
**Объект проверки:** Java backend CafeHelp и его интеграционные границы

---

## Прогресс выполнения

### 31 июля 2026 года — B-01 выполнен частично

- [x] Установлена причина checksum mismatch: исторический hash был рассчитан по исходным CRLF-байтам, а новый runner сравнивал его с нормализованным LF.
- [x] Добавлена обратная совместимость checksum без разрешения фактического изменения SQL.
- [x] Удаляется UTF-8 BOM перед исполнением baseline без изменения исторического checksum.
- [x] Alignment-миграция поддерживает как identity, так и sequence-backed идентификаторы legacy-схемы.
- [x] Основная и tax DB создаются с нуля в отдельных PostgreSQL 16 Testcontainers.
- [x] `gradlew test` больше не зависит от локальных баз на портах 5432/5433.
- [x] jOOQ generation отключён при обычной компиляции и остаётся отдельной ручной задачей.
- [x] Автоматический migration/context smoke test проходит.
- [x] Анонимный доступ к `/warehouses/**` закрыт.
- [x] Чтение складского каталога разрешено `WORKER` и `OWNER`, изменяющие операции — только `OWNER`.
- [x] VK API работает fail-closed при отсутствующем токене и использует `VK_BOT_API_TOKEN`.
- [x] Для складской матрицы доступа и VK fail-closed добавлены регрессионные тесты.
- [x] Создание заказа и его позиций выполняется в одной транзакции.
- [x] Нехватка продукта не блокирует заказ: доступная часть списывается до нуля, остаток дефицита показывает инвентаризационный отчёт.
- [x] Техническая ошибка записи позиции откатывает заказ, позиции и выполненное в транзакции складское списание.
- [x] Повторная и параллельная оплата не выполняет складское списание дважды.
- [ ] Полностью перенести оставшийся DDL из `DatabaseSchemaInitializer` в версионируемые миграции.
- [ ] Добавить отдельную проверку восстановления после намеренно ошибочной миграции.

---

## 1. Назначение документа

Документ фиксирует полный план проверки backend CafeHelp, чтобы работа могла выполняться последовательно, а важные сценарии не потерялись между отдельными задачами.

Основные цели проверки:

- убедиться, что критические операции с заказами, оплатами, складами, сменами и долгами выполняются корректно и атомарно;
- проверить разграничение доступа между `OWNER`, `WORKER`, внутренними сервисами и неавторизованными пользователями;
- сделать запуск приложения и базы данных воспроизводимым;
- определить минимальный набор автоматических тестов для безопасного дальнейшего развития;
- проверить устойчивость интеграций с PostgreSQL, налоговым контуром, MinIO, VK-ботом и Python-сервисом;
- сформировать реестр дефектов и улучшений с приоритетами.

На данном этапе **не выполняется оценка качества прогнозных и генеративных алгоритмов PyModule**. Для Python-сервиса проверяются только API-контракты, авторизация, доступность, тайм-ауты, обработка ошибок и поведение Java backend при недоступности сервиса.

---

## 2. Текущий контур backend

По результатам первичного просмотра проект содержит:

| Компонент | Назначение |
|---|---|
| Spring Boot 3.5.7 / Java 21 | Основное backend-приложение |
| Spring Security + JWT | Авторизация и роли `OWNER` / `WORKER` |
| jOOQ | Доступ к основной базе данных |
| PostgreSQL | Данные продаж, меню, складов, смен, сотрудников и клиентов |
| Отдельная PostgreSQL tax DB | Налоговые чеки и связанные операции |
| SQL migrations + `DatabaseSchemaInitializer` | Обновление структуры базы данных при старте |
| MinIO | Хранение загружаемых файлов и изображений |
| Python / ML service | Аналитика, прогнозы и ML-функции |
| VK bot | Привязка клиента и получение информации о заказах |
| Tax outbox и dispatch-контур | Надёжная передача данных о чеках |
| OpenAPI / Swagger UI | Просмотр и ручная проверка API |

Ориентировочный объём Java backend на момент просмотра:

- 24 REST-контроллера;
- около 150 HTTP endpoint;
- 38 сервисных классов;
- 72 DTO;
- 12 миграций основной БД и 1 миграция tax DB;
- 1 минимальный `contextLoads`-тест и вспомогательный `TestRunner` без проверочных утверждений.

---

## 3. Шкала приоритетов

| Приоритет | Значение |
|---|---|
| **P0 — критический** | Риск несанкционированного доступа, потери или рассинхронизации данных, невозможность надёжно запустить систему |
| **P1 — высокий** | Ошибка важного бизнес-сценария, отсутствие обязательной проверки или существенный эксплуатационный риск |
| **P2 — средний** | Проблема сопровождения, диагностики, качества API или будущего развития |
| **P3 — низкий** | Локальное улучшение качества без непосредственного влияния на работу пользователей |

---

## 4. Предварительные находки

Это результаты первичного просмотра, а не окончательный аудит. Каждую находку необходимо подтвердить отдельным воспроизводимым тестом.

### 4.1. P0 — складские endpoint разрешены без авторизации

**Статус на 31 июля 2026 года:** исправлено и покрыто integration-тестами.
Анонимный доступ запрещён; безопасные операции чтения доступны `WORKER` и
`OWNER`, изменения и инвентаризация — только `OWNER`.

В `SecurityConfig` пути `/warehouses` и `/warehouses/**` имеют `permitAll()`. При этом `WareHouseController` содержит не только чтение, но и:

- создание, изменение и удаление склада;
- назначение главного склада;
- добавление товаров;
- прямую корректировку остатков продуктов и заготовок;
- применение фактических остатков инвентаризации.

**Риск:** любой клиент, имеющий сетевой доступ к backend, потенциально может изменять складские данные без JWT.

**Что сделать при проверке:**

1. Вызвать каждый складской endpoint без токена.
2. Повторить с токенами `WORKER` и `OWNER`.
3. Согласовать матрицу прав.
4. После исправления закрепить матрицу автоматическими security-тестами.

### 4.2. P0 — оплата заказа и списание остатков могут рассинхронизироваться

**Статус на 31 июля 2026 года:** исправлено и покрыто integration-тестами.

Зафиксировано бизнес-правило:

- нехватка продукта или заготовки не отменяет заказ и оплату;
- со склада списывается всё доступное количество без ухода в отрицательный остаток;
- недостающая часть рассчитывается по снимку смены, движениям и продажам и отображается в инвентаризационном отчёте как `shortageQty`;
- ошибка БД или другая техническая ошибка откатывает заказ и позиции целиком;
- повторная или параллельная отметка одного заказа оплаченным не приводит к повторному списанию.

До исправления в `OrderService` присутствовали опасные последовательности:

- `createOrder` не отмечен `@Transactional`, хотя создаёт заказ, позиции заказа и может списывать остатки;
- ошибка складского списания при создании оплаченного заказа перехватывается и только выводится в лог;
- `updateOrderPayment` сначала помечает заказ оплаченным, а ошибку последующего списания также подавляет;
- при добавлении блюда в оплаченный заказ складское списание и запись позиции не образуют единый гарантированный сценарий.

**Риск:** заказ может считаться оплаченным, но продукты не будут списаны; также возможны частично созданные заказы и некорректные остатки.

**Что сделать при проверке:**

1. Создать оплаченный заказ при достаточном остатке.
2. Повторить при недостаточном остатке.
3. Искусственно вызвать ошибку после создания заказа, но до записи всех позиций.
4. Проверить повторную отправку одинакового запроса.
5. Проверить параллельную оплату одного заказа.
6. Зафиксировать ожидаемое правило: либо вся операция успешна, либо все изменения откатываются.

### 4.3. P0 — текущий набор миграций не даёт надёжной воспроизводимости

**Статус на 31 июля 2026 года:** основной блок исправлен и закреплён
контейнерным smoke-тестом. Осталось удалить дублирование между миграциями и
`DatabaseSchemaInitializer`.

Первичный запуск тестов остановился на несовпадении checksum для уже применённой миграции:

`db/migrations/2026-01-01_sales_baseline.sql`.

Дополнительно в `docs/database-migrations.md` уже отмечено, что текущий набор миграций не создаёт всю основную схему с нуля и предполагает наличие legacy-схемы `sales`.

При старте также запускается большой `DatabaseSchemaInitializer`, который отдельно выполняет DDL, создаёт ограничения и меняет данные. В результате структура БД формируется одновременно двумя механизмами.

**Риск:** новая среда может не подняться с нуля, а разные установки могут получить различающиеся схемы.

**Что сделать при проверке:**

1. Зафиксировать эталонную структуру рабочей базы.
2. Не изменять уже применённые миграции; все корректировки оформлять новыми файлами.
3. Поднять пустую основную БД и пустую tax DB.
4. Применить только версионируемые миграции.
5. Сравнить полученную схему с эталоном.
6. Перенести изменения из `DatabaseSchemaInitializer` в миграции и оставить один механизм управления схемой.

### 4.4. P0/P1 — VK API может работать без секретного токена

**Статус на 31 июля 2026 года:** исправлено. При отсутствии секрета API
возвращает `503`, при неверном токене — `401`; сравнение выполняется постоянным
по времени способом. Удалена ошибочная связь bot token с `VK_GROUP_TOKEN`.

Пути `/api/vk-bot/**` разрешены без JWT. Контроллер выполняет собственную проверку заголовка, но если настройка токена пустая, проверка полностью пропускается.

Кроме того, контроллер читает `vk.bot.api-token`, а в `application.properties` это свойство связано с `VK_GROUP_TOKEN`, хотя в Docker отдельно передаётся `VK_BOT_API_TOKEN`.

**Риск:** при пустой или неверно связанной настройке внешнему клиенту могут стать доступны операции привязки и сведения о заказах клиента.

**Что сделать при проверке:**

1. Запустить backend без VK-токенов и проверить все `/api/vk-bot/**`.
2. Проверить запуск с `VK_BOT_API_TOKEN`, но без `VK_GROUP_TOKEN`.
3. Убедиться, что приложение не стартует либо закрывает API при отсутствии обязательного секрета.
4. Использовать постоянное по времени сравнение секретов и добавить ограничение частоты запросов.

### 4.5. P1 — секреты и пароли частично находятся в конфигурации проекта

В `application.properties`, `docker-compose.yml` и настройке jOOQ присутствуют фиксированные пароли PostgreSQL и стандартные данные MinIO. JWT и internal service token в Docker уже сделаны обязательными, но локальная конфигурация остаётся неоднородной.

**Риск:** повторное использование тестовых значений в доступной среде, случайное раскрытие секретов и сложная ротация.

**Что сделать при проверке:**

- составить перечень всех секретов;
- вынести их в переменные окружения или secret storage;
- убрать реальные ИНН, пароли и ключи из общей конфигурации;
- добавить безопасный `.env.example` без значений;
- проверить, что production-профиль не запускается с тестовыми значениями;
- после изменения настроек выполнить ротацию ранее использованных секретов.

### 4.6. P1 — внутренний service token получает полные права `OWNER`

`ServiceTokenAuthenticationFilter` создаёт аутентификацию с ролью `OWNER`. Это даёт внутреннему токену доступ не только к нужным интеграционным endpoint, но потенциально ко всему административному API.

**Что сделать при проверке:**

- составить список endpoint, которые действительно нужны PyModule и другим сервисам;
- ввести отдельную роль, например `INTERNAL_SERVICE`;
- ограничить токен конкретными маршрутами;
- проверить отсутствие доступа к сотрудникам, аккаунтам, складам и административным операциям;
- предусмотреть ротацию токена и аудит его использования.

### 4.7. P1 — деактивация пользователя не отзывает ранее выданный JWT

При логине проверяется активность аккаунта, но на последующих запросах JWT принимается без повторной проверки текущего статуса и роли пользователя в БД. Срок жизни токена по умолчанию — 8 часов.

**Риск:** деактивированный сотрудник или пользователь с изменённой ролью сохраняет старые права до истечения токена.

**Что сделать при проверке:**

1. Авторизовать пользователя и сохранить токен.
2. Деактивировать аккаунт.
3. Проверить доступ старым токеном.
4. Изменить роль и повторить проверку.
5. Выбрать механизм отзыва: короткий access token, token version, denylist либо проверка аккаунта для чувствительных операций.

### 4.8. P1 — публичный bootstrap требует проверки гонки и условий отключения

`/api/auth/bootstrap` публичен и закрывается после появления первого аккаунта. Проверка количества аккаунтов и создание владельца не образуют явно защищённую от параллельного выполнения операцию.

**Что сделать при проверке:**

- отправить несколько параллельных bootstrap-запросов в пустую БД;
- проверить, что создаётся только один владелец;
- проверить невозможность повторного bootstrap после создания аккаунта;
- определить безопасный способ первичной инициализации: одноразовый setup token, CLI-команда или обязательный deployment secret.

### 4.9. P1 — нет единого контракта валидации и ошибок

Зависимость Bean Validation подключена, но в просмотренных DTO и контроллерах не используются `@Valid`, `@NotNull`, `@NotBlank`, `@Positive` и аналогичные ограничения. Проверки выполняются вручную и различаются между endpoint.

Исключения обрабатываются неодинаково:

- часть endpoint возвращает `null`;
- часть — строку;
- часть — `Map` с ключом `message` или `error`;
- часть преобразует любую ошибку в HTTP 500;
- местами наружу возвращается `e.getMessage()`, что может раскрыть внутренние детали;
- глобальный обработчик есть только для ошибок Python-сервиса.

**Что сделать при проверке:**

- определить единый формат ошибки;
- добавить валидацию входных DTO;
- ввести глобальный обработчик ожидаемых ошибок;
- разделить `400`, `401`, `403`, `404`, `409`, `422`, `500`, `502/503`;
- проверить отсутствие stack trace, SQL и внутренних URL в ответах.

Рекомендуемый формат:

```json
{
  "code": "INSUFFICIENT_STOCK",
  "message": "Недостаточно товара на складе",
  "fieldErrors": [],
  "traceId": "..."
}
```

### 4.10. P1 — автоматическое тестовое покрытие практически отсутствует

Текущий тестовый набор содержит только загрузку Spring-контекста. `TestRunner` печатает данные в консоль, не содержит утверждений и запускается вместе с тестовым контекстом, из-за чего тест зависит от состояния БД.

**Что сделать при проверке:**

- заменить консольные проверки на изолированные JUnit-тесты;
- не использовать рабочую или разработческую БД;
- добавить Testcontainers для PostgreSQL;
- построить тесты по рискам, начиная с безопасности и денежных/складских операций;
- обеспечить один воспроизводимый запуск всех тестов командой Gradle.

### 4.11. P1 — выплаты зарплаты пока не являются backend-данными

История выплат сотрудникам хранится во frontend в `localStorage` под ключом `salaryPayments`.

**Риск:** данные теряются при очистке браузера, не синхронизируются между рабочими местами и не дают надёжного аудита.

**Что сделать при проверке и развитии:**

- определить сущность выплаты: сотрудник, сумма, дата, автор операции, комментарий, период;
- хранить выплаты в PostgreSQL;
- выполнять выплату через owner-only endpoint;
- запретить отрицательные и нулевые суммы;
- проверить частичные выплаты и действие «выдать всё»;
- добавить журнал изменений и защиту от повторной отправки.

### 4.12. P2 — закрытие и изменение смены требуют проверки транзакционности

**Статус на 31.07.2026: исправлено и покрыто интеграционными тестами.**

`closeShift` и `updateShift` выполняются транзакционно. Закрытие блокирует строку смены до фиксации итогов; создание заказа, его оплата и остальные изменяющие операции используют ту же блокировку. Поэтому заказ либо полностью фиксируется до закрытия и попадает в итог, либо получает конфликт закрытой смены.

Зафиксированы правила:

- доход и себестоимость включают только оплаченные и неотменённые заказы;
- неоплаченные и долговые заказы отображаются в Z-отчёте отдельно и не считаются полученной выручкой;
- цена и себестоимость фиксируются в строке заказа и не меняются задним числом после редактирования меню;
- отрицательные и `null`-расходы отклоняются;
- повторное и параллельное закрытие идемпотентно и возвращает первый сохранённый результат;
- общий endpoint изменения смены не принимает от клиента время закрытия и финансовые итоги;
- кассовый интерфейс закрывает смену через `/api/shifts/{id}/close`, а не рассчитывает итог в браузере.

### 4.13. P2 — CORS настроен в нескольких местах

CORS одновременно задаётся в `SecurityConfig`, отдельной конфигурации и аннотациях контроллеров. Разрешён только `http://localhost:3000`.

**Риск:** расхождения между средами, непредсказуемое поведение preflight и ручные изменения при каждом deployment.

**Что сделать:** оставить один конфиг, вынести origins в переменные окружения и добавить тесты для разрешённого и запрещённого origin.

### 4.14. P2 — backend не имеет полноценной эксплуатационной диагностики

В Docker есть healthcheck для PostgreSQL и PyModule, но не для Java backend. Spring Boot Actuator и прикладные метрики не подключены.

**Что сделать:**

- добавить readiness и liveness;
- проверять отдельно доступность основной БД и tax DB;
- добавить метрики ошибок, времени ответа, пула соединений, outbox и внешних интеграций;
- использовать структурированные логи и `traceId`;
- не включать секреты и персональные данные в логи.

### 4.15. Положительные элементы, которые следует сохранить

- stateless-аутентификация и BCrypt для паролей;
- JWT secret обязателен при Docker-запуске;
- сравнение internal service token выполняется постоянным по времени способом;
- в движениях, проводке накладных и части операций уже применяются транзакции;
- tax-контур использует outbox, повторные попытки и `FOR UPDATE SKIP LOCKED`;
- миграционный механизм проверяет checksum и тем самым обнаруживает изменение применённых файлов;
- основная и налоговая базы разделены.

---

## 5. Общая стратегия проверки

Проверка строится снизу вверх:

1. **Воспроизводимость окружения** — чистый запуск, миграции и тестовые данные.
2. **Безопасность API** — аутентификация, роли и внешние токены.
3. **Контракты API** — входные данные, статусы, ответы и ошибки.
4. **Бизнес-логика** — заказы, оплаты, смены, склад, долги, зарплата.
5. **Интеграции** — tax DB, налоговый провайдер, MinIO, VK и Python.
6. **Конкурентность** — повторные и параллельные операции.
7. **Производительность и наблюдаемость** — время ответа, метрики и диагностика.
8. **Регрессия** — автоматизация критических сценариев в CI.

Для каждого дефекта должны быть:

- воспроизводимый сценарий;
- ожидаемый и фактический результат;
- оценка влияния;
- приоритет;
- предлагаемое исправление;
- автоматический регрессионный тест.

---

## 6. Декомпозиция работ

### B-00. Зафиксировать проверяемую версию

**Приоритет:** P0  
**Оценка:** 0,5 дня

- [ ] Зафиксировать commit и список локальных незакоммиченных изменений.
- [ ] Сохранить версии Java, Gradle, Docker и PostgreSQL.
- [ ] Зафиксировать используемые переменные окружения без значений секретов.
- [ ] Сохранить OpenAPI JSON как снимок текущего контракта.
- [ ] Определить отдельную тестовую конфигурацию.

**Результат:** воспроизводимая точка начала аудита.

### B-01. Восстановить воспроизводимый запуск БД и тестов

**Приоритет:** P0  
**Оценка:** 1–2 дня

- [ ] Разобрать checksum mismatch baseline-миграции.
- [ ] Сопоставить историю миграций с файлами в репозитории.
- [ ] Подготовить полный baseline для пустой основной БД.
- [ ] Проверить создание tax DB с нуля.
- [ ] Перенести runtime DDL из `DatabaseSchemaInitializer` в миграции.
- [ ] Проверить повторный запуск миграций без изменений.
- [ ] Проверить rollback/восстановление после ошибочной миграции.
- [ ] Отделить jOOQ code generation от локальной рабочей базы.
- [ ] Добавить автоматический migration smoke test.

**Критерий готовности:** чистая среда поднимается одной документированной командой, а повторный запуск не изменяет схему.

### B-02. Составить реестр API и матрицу доступа

**Приоритет:** P0  
**Оценка:** 1 день

- [ ] Выгрузить все endpoint из OpenAPI и сверить с контроллерами.
- [ ] Для каждого endpoint указать назначение и владельца бизнес-функции.
- [ ] Назначить ожидаемый доступ: public, `WORKER`, `OWNER`, internal service.
- [ ] Отдельно проверить `/warehouses/**`, `/api/vk-bot/**`, Swagger и bootstrap.
- [ ] Найти endpoint, попадающие только под `.anyRequest().authenticated()`.
- [ ] Проверить все методы `GET`, `POST`, `PUT`, `PATCH`, `DELETE` для каждой роли.

**Результат:** таблица «endpoint → разрешённые роли → фактический результат».

### B-03. Проверить аутентификацию и жизненный цикл аккаунта

**Приоритет:** P0/P1  
**Оценка:** 1–1,5 дня

- [ ] Успешный и неуспешный login.
- [ ] Неактивный аккаунт.
- [ ] Неверная подпись JWT.
- [ ] Истёкший JWT.
- [ ] Изменённая роль после выдачи JWT.
- [ ] Деактивация после выдачи JWT.
- [ ] Параллельный bootstrap.
- [ ] Повторный bootstrap.
- [ ] Создание дублирующего username.
- [ ] Создание нескольких аккаунтов для одного сотрудника.
- [ ] Требования к минимальной сложности пароля.
- [ ] Ограничение частоты попыток входа и bootstrap.

**Критерий готовности:** тесты однозначно подтверждают матрицу ролей и отзыв доступа.

### B-04. Проверить входные данные и единый формат ошибок

**Приоритет:** P1  
**Оценка:** 1–2 дня

- [ ] Выделить request DTO отдельно от response DTO для изменяющих операций.
- [ ] Добавить declarative validation.
- [ ] Проверить пустое body, некорректный JSON и неизвестные поля.
- [ ] Проверить `null`, пустые строки, нулевые и отрицательные ID.
- [ ] Проверить `NaN`, бесконечность, отрицательные суммы и количества.
- [ ] Проверить слишком длинные строки и Unicode.
- [ ] Проверить даты в прошлом/будущем и неверные диапазоны.
- [ ] Ввести единый глобальный exception handler.
- [ ] Исключить возврат внутренних exception message клиенту.
- [ ] Проверить корректность HTTP-статусов.

**Критерий готовности:** одинаковые классы ошибок имеют одинаковый JSON-контракт во всём API.

### B-05. Проверить заказы, оплату и печатные данные

**Приоритет:** P0  
**Оценка:** 2–3 дня

- [ ] Создание заказа без клиента.
- [ ] Создание заказа для существующего и несуществующего клиента.
- [ ] Заказ с одним блюдом, набором и несколькими позициями.
- [ ] Пустой заказ и позиции с некорректным количеством.
- [ ] Сумма заказа: серверный пересчёт против значения от клиента.
- [ ] Наличные, перевод, долг и неоплаченный заказ.
- [ ] Переход из неоплаченного в оплаченный.
- [ ] Повторная отметка оплаты.
- [ ] Недостаток товара при оплате.
- [ ] Частичная ошибка записи позиций.
- [ ] Повторный запрос создания заказа.
- [ ] Параллельная оплата одного заказа.
- [ ] Выдача заказа и повторная выдача.
- [ ] Kitchen payload для корректного и некорректного заказа.
- [ ] Проверка того, что печать не меняет бизнес-состояние заказа.
- [ ] Проверка создания tax outbox только для нужных способов оплаты.

#### Что потребуется для редактирования и удаления созданного заказа

**Статус на 31 июля 2026 года:** реализован первый безопасный этап.

- добавлен `PUT /api/orders/{orderId}` для полной замены состава и редактируемых реквизитов заказа;
- сумма позиций при редактировании пересчитывается на backend по актуальным ценам;
- добавлен идемпотентный `DELETE /api/orders/{orderId}` с мягкой отменой и сохранением причины;
- редактирование и отмена разрешены только для неоплаченного, невыданного заказа открытой смены;
- поле `version` защищает от сохранения поверх более новой версии заказа;
- отменённые заказы исключены из активных списков, долгов, закрытия смены, инвентаризационного отчёта, ML-аналитики и повторного создания tax outbox;
- операции покрыты integration- и HTTP-тестами.

Остаётся отдельный второй этап: корректировка оплаченного заказа. До появления журнала фактического складского списания и возвратного фискального сценария backend намеренно отвечает `409 Conflict`, а не выполняет потенциально неверную складскую или финансовую корректировку.

**Зафиксированное бизнес-правило:** продукты и заготовки, уже использованные для оплаченного или переданного в производство заказа, физически на склад не возвращаются. При финансовой отмене расход должен сохраниться и быть переклассифицирован в отдельное документированное списание — например, «отмена заказа», «брак» или «потери». Денежный возврат и складской расход являются разными операциями.

1. Согласовать матрицу разрешённых действий по состояниям заказа:
   - неоплаченный и невыданный заказ разрешить редактировать и отменять;
   - для оплаченного, выданного заказа или заказа закрытой смены определить отдельный сценарий корректировки;
   - ограничить операции подходящими ролями и не полагаться только на скрытие кнопок во frontend.
2. Добавить отдельные request DTO и API-контракты:
   - `PATCH`/`PUT` для изменения реквизитов и полного состава заказа;
   - операцию отмены заказа вместо безусловного физического удаления;
   - обязательную причину отмены или корректировки для оплаченного заказа.
3. Выполнять изменение состава заказа, пересчёт суммы, складскую корректировку и изменение связанных данных в одной транзакции:
   - для неоплаченного заказа склад не изменять;
   - для оплаченного заказа вычислять разницу между старым и новым составом;
   - добавленные позиции списывать дополнительно;
   - удалённые позиции или уменьшенное количество не возвращать в физический остаток, а учитывать как отдельный расход/потери;
   - при нехватке добавленных продуктов не блокировать изменение, а сохранять дефицит для инвентаризационного отчёта;
   - при технической ошибке откатывать всю операцию.
4. Не удалять оплаченные заказы физически. Использовать статус отмены и компенсирующие операции:
   - не увеличивать складской остаток на ранее списанные продукты и заготовки;
   - исключить отменённую продажу из выручки, сохранив использованные ингредиенты как отдельное складское списание;
   - учитывать это списание в инвентаризационном отчёте, чтобы ожидаемый остаток не увеличивался искусственно;
   - сформировать корректирующий или возвратный фискальный документ через tax outbox;
   - не позволять удалению нарушать отчёт уже закрытой смены.
5. Добавить историю изменений заказа: кто, когда и почему изменил заказ, старое и новое состояние, изменение суммы, оплаты и складских остатков.
6. Защитить редактирование от параллельных запросов: блокировка строки заказа или поле версии, идемпотентность отмены, отсутствие повторного списания или повторной проводки потерь.
7. Добавить frontend-редактор состава заказа с серверным пересчётом суммы, подтверждением удаления и явным отображением ограничений для оплаченного или выданного заказа.
8. Закрепить integration-тестами:
   - редактирование и отмену неоплаченного заказа;
   - добавление, удаление и изменение количества позиции оплаченного заказа;
   - частичный складской остаток и отражение дефицита в инвентаризации;
   - отмену оплаченного заказа без увеличения физического остатка и с отдельной проводкой расхода;
   - запрет изменения закрытой смены;
   - повторную и параллельную отмену;
   - откат заказа, позиций, суммы, склада и outbox при технической ошибке.

**Критерий готовности:** заказ, позиции, оплата, склад и outbox сохраняются согласованно.

### B-06. Проверить склады, остатки и движения

**Приоритет:** P0  
**Оценка:** 2–3 дня

**Статус на 31 июля 2026 года:** конкурентное изменение остатков исправлено и покрыто integration-тестами.

- операции изменения продукта и заготовки блокируют склад и соответствующие строки остатков до завершения транзакции;
- перемещение блокирует оба склада в стабильном порядке, поэтому встречные перемещения не образуют взаимную блокировку;
- два параллельных списания больше не могут использовать один и тот же исходный остаток;
- ручное документальное списание при нехватке отклоняется целиком и не создаёт частичный остаток, документ или `stock_movements`;
- правило частичного списания сохранено только для продажи: заказ не блокируется дефицитом, а доступный остаток списывается до нуля;
- `NaN` и бесконечные количества отклоняются до записи в БД.

- [ ] Создание, изменение и удаление склада.
- [ ] Ровно один главный склад.
- [ ] Удаление главного или используемого склада.
- [ ] Приход, расход и перемещение между складами.
- [ ] Нулевое и отрицательное количество.
- [x] Недостаточный остаток.
- [ ] Повторная проводка документа.
- [x] Параллельное списание одного остатка.
- [ ] Единицы измерения и `unit_factor`.
- [ ] Списание продуктов и заготовок по техкарте.
- [ ] Инвентаризация и применение фактических остатков.
- [x] Формирование отчёта по смене.
- [ ] Целостность `stock_movements`, document lines и текущего остатка.

**Критерий готовности:** отрицательные и двойные списания невозможны без явно разрешённого бизнес-правила.

### B-07. Проверить смены и финансовые итоги

**Приоритет:** P1  
**Оценка:** 1,5–2 дня

- [x] Открытие смены одним и несколькими сотрудниками.
- [x] Запрет двух открытых смен одного сотрудника.
- [x] Параллельное открытие смен.
- [x] Изменение состава сотрудников.
- [x] Закрытие пустой смены.
- [x] Расчёт дохода, себестоимости, расходов и прибыли.
- [x] Отрицательные и `null`-расходы.
- [x] Заказ, добавленный во время закрытия.
- [x] Повторное закрытие смены.
- [x] Z-отчёт до и после закрытия.
- [x] Складской snapshot и инвентаризационный отчёт.

**Критерий готовности:** финансовый итог детерминирован, а закрытая смена не меняется неявно.

**Результат:** критерий выполнен. Строка смены и строки сотрудников блокируются в стабильном порядке, финансовый итог рассчитывается только сервером, а закрытая смена защищена от создания, оплаты и изменения заказов. Z-отчёт сохраняет все активные заказы для контроля, но разделяет оплаченные и неоплаченные суммы.

### B-08. Проверить клиентов, долги и VK-привязку

**Приоритет:** P1  
**Оценка:** 1,5–2 дня

- [ ] Создание и поиск клиента.
- [ ] Дубликаты клиента и правила идентификации.
- [ ] Долг без клиента.
- [ ] Дата погашения долга.
- [ ] Частичная и полная оплата долга.
- [ ] Повторное погашение.
- [ ] Сегодняшние и просроченные долги на границе даты/часового пояса.
- [ ] Генерация одноразового кода VK.
- [ ] Истечение, повторное использование и перебор кода.
- [ ] Привязка одного VK ID к нескольким клиентам.
- [ ] Отвязка и повторная привязка.
- [ ] Доступ VK-бота к чужому клиенту.

**Критерий готовности:** изменение долга и VK-привязки имеет понятную историю и не допускает доступ к чужим данным.

### B-09. Спроектировать и проверить backend-контур выплат сотрудникам

**Приоритет:** P1  
**Оценка:** 1–2 дня на проектирование и тесты без учёта UI

- [ ] Согласовать источник суммы к выплате.
- [ ] Описать частичную выплату и действие «выдать всё».
- [ ] Создать таблицу истории выплат.
- [ ] Ограничить операции ролью `OWNER`.
- [ ] Хранить автора и время выплаты.
- [ ] Запретить сумму больше доступной, если это бизнес-правило.
- [ ] Добавить idempotency key для защиты от двойного клика.
- [ ] Проверить параллельные выплаты.
- [ ] Предусмотреть отмену/корректировку отдельной проводкой, а не удалением истории.
- [ ] Перенести данные из `localStorage` либо явно начать новую серверную историю.

**Критерий готовности:** выплаты сохраняются централизованно и одинаково видны на всех рабочих местах.

### B-10. Проверить справочники и технологические карты

**Приоритет:** P1/P2  
**Оценка:** 1,5–2 дня

- [ ] Сотрудники и архивирование.
- [ ] Поставщики и привязка продуктов.
- [ ] Товары и единицы измерения.
- [ ] Категории блюд.
- [ ] Блюда, наборы и цены.
- [ ] Технологические карты.
- [ ] Заготовки и вложенные ингредиенты.
- [ ] Циклические ссылки в рецептах.
- [ ] Удаление используемого справочного объекта.
- [ ] Изменение цены после создания старого заказа.
- [ ] Проверка legacy-поля зарплаты: удалить, мигрировать или документировать его окончательную семантику.

### B-11. Проверить налоговый контур и outbox

**Приоритет:** P0/P1  
**Оценка:** 2–3 дня

- [ ] Создание outbox-записи вместе с бизнес-операцией.
- [ ] Уникальность event key и повторная отправка.
- [ ] Параллельная обработка несколькими worker.
- [ ] Поведение `FOR UPDATE SKIP LOCKED`.
- [ ] Сбой между записью в tax DB и `markProcessed`.
- [ ] Истечение lock для зависшей записи.
- [ ] Retry, backoff и максимальное число попыток.
- [ ] Dead-letter/ручное восстановление.
- [ ] Недоступная tax DB.
- [ ] Тайм-аут и некорректный ответ внешнего партнёра.
- [ ] Маскирование ИНН, токенов и фискальных данных в логах.
- [ ] Сверка оплаченных заказов с outbox и tax DB.
- [ ] Проверка безопасного режима без реальной отправки чеков.

**Критерий готовности:** повторная доставка безопасна, потерянные и зависшие чеки обнаруживаются.

### B-12. Проверить MinIO и загрузку файлов

**Приоритет:** P1  
**Оценка:** 1 день

- [ ] Допустимые MIME-типы и расширения.
- [ ] Максимальный размер файла.
- [ ] Пустые и повреждённые файлы.
- [ ] Подмена Content-Type.
- [ ] Безопасное имя объекта и отсутствие path traversal.
- [ ] Недоступность MinIO.
- [ ] Удаление/замена файла и очистка неиспользуемых объектов.
- [ ] Доступность публичного URL только там, где это допустимо.
- [ ] Нестандартные MinIO credentials во всех внешних средах.

### B-13. Проверить контракт с PyModule

**Приоритет:** P1  
**Оценка:** 1–2 дня

- [ ] Составить список Java → Python и Python → Java вызовов.
- [ ] Зафиксировать схемы запросов и ответов.
- [ ] Проверить internal service token.
- [ ] Проверить connect/read timeout.
- [ ] Проверить недоступность и медленный ответ.
- [ ] Проверить некорректный JSON и частичный ответ.
- [ ] Проверить несовместимую версию контракта.
- [ ] Убедиться, что сбой аналитики не блокирует кассу, склад и смены.
- [ ] Проверить health endpoint.

**Примечание:** качество прогнозов и генеративных алгоритмов оценивается отдельным планом.

### B-14. Проверить производительность и конкурентность

**Приоритет:** P1/P2  
**Оценка:** 2–3 дня

- [ ] Определить ожидаемое число рабочих мест и запросов.
- [ ] Измерить p50, p95 и p99 для ключевых endpoint.
- [ ] Проверить N+1 и повторные запросы в циклах.
- [ ] Проверить крупные списки заказов, клиентов и движений.
- [ ] Добавить пагинацию и ограничения `limit`.
- [ ] Проверить индексы по фактическим запросам.
- [ ] Проверить пул соединений основной и tax DB.
- [ ] Провести конкурентные тесты оплаты, склада, смен и outbox.
- [ ] Зафиксировать допустимые пороги времени ответа.

### B-15. Проверить логи, метрики и восстановление

**Приоритет:** P2  
**Оценка:** 1–2 дня

- [ ] Убрать `System.out`, `System.err` и `printStackTrace`.
- [ ] Настроить единый logger и структурированный формат.
- [ ] Добавить request/trace ID.
- [ ] Добавить backend readiness/liveness.
- [ ] Добавить метрики HTTP, БД, outbox и интеграций.
- [ ] Настроить алерты на накопление outbox и рост 5xx.
- [ ] Проверить резервное копирование обеих БД и MinIO.
- [ ] Провести тестовое восстановление.
- [ ] Подготовить краткий runbook для типовых сбоев.

### B-16. Автоматизировать проверки в CI

**Приоритет:** P1  
**Оценка:** 1–2 дня после создания тестов

- [ ] `compileJava` и unit tests.
- [ ] Integration tests с чистыми контейнерами.
- [ ] Проверка миграций.
- [ ] Проверка OpenAPI на нежелательные breaking changes.
- [ ] Статический анализ и dependency scan.
- [ ] Проверка отсутствия секретов.
- [ ] Сбор отчётов тестов.
- [ ] Запрет merge при провале обязательных проверок.

---

## 7. Матрица обязательных сценариев

| Контур | Happy path | Негативный сценарий | Права | Конкурентность |
|---|---|---|---|---|
| Авторизация | Корректный login | Неверный пароль, disabled user, expired JWT | Public / Worker / Owner | Параллельный bootstrap |
| Заказы | Создание и выдача | Пустые позиции, неверная смена | Worker / Owner | Двойное создание и оплата |
| Оплата | Cash / transfer / debt | Недостаток остатка | Worker / Owner | Двойная оплата |
| Склад | Приход и списание | Отрицательный остаток | Только согласованные роли | Параллельное списание |
| Смены | Открытие и закрытие | Повторное закрытие | Worker / Owner | Две открытые смены |
| Клиенты | Создание и поиск | Дубликаты | Worker / Owner | Одновременное изменение долга |
| Зарплата | Частичная выплата | Нулевая/лишняя сумма | Owner | Двойная выплата |
| VK | Привязка и история | Неверный/пустой токен | Только bot token | Повтор кода |
| Tax | Успешная доставка | Тайм-аут и 5xx | Owner / internal | Несколько relay worker |
| MinIO | Загрузка изображения | Большой/опасный файл | Авторизованный доступ | Повторная загрузка |
| PyModule | Валидный ответ | Timeout/битый JSON | Internal service | Серия медленных запросов |

---

## 8. Тестовая среда и данные

### 8.1. Изоляция

- отдельные PostgreSQL-контейнеры для основной и tax DB;
- отдельный MinIO bucket;
- mock-сервер внешнего налогового партнёра;
- управляемый stub PyModule для тестов Java backend;
- никакого подключения к production и рабочей локальной БД разработчика;
- автоматическая очистка данных между тестами.

### 8.2. Минимальные фикстуры

- один активный `OWNER`;
- один активный `WORKER`;
- один disabled account;
- несколько сотрудников без аккаунтов;
- главный и дополнительный склады;
- товары с разными единицами и коэффициентами;
- блюдо, набор и заготовка;
- клиент без долга и клиент с долгом;
- открытая и закрытая смены;
- оплаченный, неоплаченный и долговой заказы;
- pending, processing, failed и processed записи outbox.

### 8.3. Рекомендуемая структура тестов

```text
src/test/java/com/shakur/cafehelp/
├── unit/          # чистая бизнес-логика без Spring и БД
├── web/           # MockMvc, контракты и security matrix
├── integration/   # PostgreSQL/Testcontainers и транзакции
├── contract/      # PyModule, VK, tax partner, MinIO
└── smoke/         # запуск полного приложения
```

Рекомендуемые средства:

- JUnit 5;
- Spring Boot Test и MockMvc;
- AssertJ;
- Testcontainers PostgreSQL;
- WireMock или MockWebServer для HTTP-интеграций;
- Awaitility для retry/outbox;
- стандартные Gradle test reports.

---

## 9. Порядок выполнения

### Волна 1 — разблокировать надёжную проверку

1. B-00: зафиксировать версию.
2. B-01: исправить воспроизводимость миграций.
3. Поднять изолированную тестовую среду.
4. Создать первый integration smoke test.

### Волна 2 — закрыть критические риски

1. B-02: матрица доступа.
2. Проверка и закрытие публичных складских операций.
3. Проверка VK-токена и bootstrap.
4. B-05 и B-06: атомарность заказа, оплаты и склада.

### Волна 3 — проверить основную бизнес-логику

1. Смены.
2. Клиенты и долги.
3. Сотрудники и выплаты.
4. Справочники и техкарты.

### Волна 4 — интеграции и эксплуатация

1. Tax/outbox.
2. PyModule.
3. MinIO и VK.
4. Производительность, метрики и восстановление.

### Волна 5 — закрепить результат

1. Автоматизировать регрессию в CI.
2. Обновить OpenAPI.
3. Подготовить итоговый отчёт.
4. Закрыть P0 и согласовать сроки P1/P2.

Ориентировочно:

- первичная проверка критических контуров — **5–8 рабочих дней**;
- полный аудит с интеграционными и конкурентными тестами — **10–15 человеко-дней**;
- исправления оцениваются отдельно после подтверждения дефектов.

---

## 10. Формат регистрации дефекта

```text
ID:
Название:
Приоритет:
Компонент / endpoint:
Версия:
Предусловия:
Шаги воспроизведения:
Ожидаемый результат:
Фактический результат:
Влияние:
Логи / traceId:
Предлагаемое исправление:
Регрессионный тест:
Статус:
```

---

## 11. Критерии завершения backend-аудита

Проверку можно считать завершённой, когда:

- backend и обе БД разворачиваются с нуля воспроизводимым способом;
- нет незакрытых подтверждённых P0;
- все endpoint внесены в матрицу доступа;
- критические сценарии заказов, оплаты, склада, смен и долгов покрыты integration-тестами;
- ошибки складского списания не могут оставить оплаченный заказ в несогласованном состоянии;
- повторные и параллельные запросы проверены для денежных и складских операций;
- интеграции имеют тесты timeout, retry и некорректного ответа;
- секреты не хранятся в общей конфигурации;
- API использует согласованную валидацию и формат ошибок;
- readiness, liveness, основные метрики и журналирование пригодны для диагностики;
- итоговый реестр содержит приоритет, решение и регрессионный тест для каждой подтверждённой проблемы.

---

## 12. Ближайшая очередь задач

Если начинать проверку сразу после этого плана, рекомендуемый порядок:

1. Восстановить корректную историю baseline-миграции и проверить пустую БД.
2. Создать Testcontainers-базу и первый smoke test.
3. Автоматически проверить всю security matrix.
4. Закрыть неавторизованный доступ к `/warehouses/**`.
5. Сделать VK API fail-closed при отсутствии токена.
6. Написать integration-тест атомарности создания и оплаты заказа.
7. Проверить параллельные складские списания.
8. Проверить закрытие смены и финансовый итог.
9. Закрепить tax outbox тестами повторной и параллельной обработки.
10. Спроектировать серверную историю выплат сотрудникам вместо `localStorage`.
