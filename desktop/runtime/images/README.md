# Offline image pack

`tools/build-windows-release.ps1` создаёт здесь `cafehelp-images.tar`. Архив не хранится в Git, но включается в offline NSIS installer и загружается launcher при первом запуске.

