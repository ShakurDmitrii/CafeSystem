# CafeSystem UI: контекст и прогресс

Этот файл хранит решения по редизайну между рабочими сессиями. Перед продолжением
нужно проверить `git status`, прочитать этот файл и не перезаписывать чужие изменения.

## Цель

Сделать интерфейс CafeSystem понятным для владельца и сотрудников кафе, не меняя
существующую бизнес-логику и API. Каждую страницу сначала аккуратно декомпозируем,
затем обновляем визуально и проверяем на десктопе, планшете и телефоне.

## Постоянные правила

- Сохранять текущие маршруты, роли `OWNER` / `WORKER` и поведение API.
- Не переписывать рабочую логику без необходимости; выносить её небольшими шагами.
- Поддерживать ширину от 320 px, safe area и удобные зоны нажатия не меньше 44 px.
- Использовать семантические элементы, подписи полей, видимый `:focus-visible`,
  понятные ошибки и `aria-live` для асинхронных сообщений.
- Учитывать `prefers-reduced-motion`; не использовать `transition: all`.
- Визуальная основа: глубокий зелёный, белый и спокойные мятные оттенки; интерфейс
  напоминает рабочую смену кафе, а не универсальный административный шаблон.
- Шрифты: Bahnschrift для акцентных заголовков, Aptos для текста, Cascadia Mono
  для числовых и служебных данных.
- Не трогать несвязанные изменения в dirty worktree.

## Готово

### Общая оболочка

- Добавлен адаптивный `AppShell` с навигацией по ролям и мобильным drawer.
- Обновлены глобальные токены, фон, типографика, focus-состояния и safe area.
- Проверены ширины 1440, 768, 390 и 320 px.

### Главная `/`

- Данные вынесены в `useHomeDashboard`.
- Интерфейс разделён на карточки метрик, быстрые действия, популярные блюда и
  складские предупреждения.
- Добавлены состояния загрузки, ошибки и пустые состояния.
- Выполнена визуальная проверка и production build.

### Вход `/login`

- API авторизации вынесен в `services/authApi.js`.
- Добавлены корректные labels, autocomplete, показ пароля, фокус первой ошибки и
  понятная обработка сетевой ошибки.
- Сделан отдельный компактный мобильный сценарий.
- Проверены ширины 1440, 768, 390 и 320 px; production build проходит.

### Меню `/dish`

- Пользовательская проверка уникальности названия набора сохранена.
- Большая JSX-разметка вынесена в `CashierPages/dish-page`, а API и обработчики
  остались в контейнере `DishPage.jsx`.
- Обновлены блюда и наборы: переключатель режимов, формы, карточки, состояния,
  фото и мобильное модальное окно.
- Техкарты теперь открываются обычными ссылками; добавлены labels, names,
  `aria-live`, размеры изображений и возврат фокуса после закрытия модального окна.
- Проверены 1440 / 768 / 390 / 320 px, режим наборов и отсутствие горизонтального
  скролла. Скриншоты находятся в `output/playwright/dish-responsive`.
- Мобильная шапка не фиксируется поверх контента; skip-link полностью скрыт до
  клавиатурного фокуса. Повторная проверка: `phone-sets-390-fixed.png`.
- Production build и `git diff --check` проходят.

### Технологические карты `/tech-card/:dishId` и `/preparation-tech-card/:preparationId`

- Большая разметка вынесена в компоненты заголовка, редактора, списка ингредиентов,
  выбора ингредиента и создания продукта; API и расчёты сохранены в контейнере.
- Сценарии блюда и заготовки разделены понятными подписями, маршрутами возврата и
  показателями «Цена продажи» / «Выход партии».
- Добавлены валидация строк, состояния сохранения и ошибки, фокус-ловушка,
  закрытие по Escape и возврат фокуса для модальных окон.
- Карточки ингредиентов сохраняют количество, отход, единицы и стоимость на
  телефоне; горизонтального скролла на 390 px нет.
- Проверены десктоп 1440 px и телефон 390 px, оба типа техкарт, выбор и создание
  ингредиента, вход в редактирование и отмена. Скриншоты находятся в
  `output/playwright/tech-card-responsive`.
- Production build и `git diff --check` проходят; ошибок в консоли браузера нет.

### Заготовки `/preparations`

- Страница разделена на сводку цеха, редактор, каталог, карточку заготовки и
  отдельные диалоги выпуска и удаления; запросы и агрегирование данных остались
  в контейнере `PreparationsPage.jsx`.
- Сохранены создание с переходом в техкарту и выпуск на склад; добавлены
  редактирование через существующий `PUT` и подтверждаемое удаление через `DELETE`.
- В карточках одновременно видны выход, себестоимость, состав, общий остаток и
  распределение по складам; пустые техкарты выделены отдельным статусом.
- Системный `alert` заменён на `aria-live`-сообщение. Формы показывают inline-ошибки,
  модальные окна удерживают фокус, закрываются по Escape и возвращают фокус.
- Проверены 1440 / 768 / 390 / 320 px, выпуск, редактирование, удаление и валидация.
  Скриншоты находятся в `output/playwright/preparations-responsive`.
- Production build и `git diff --check` проходят; ошибок в консоли браузера нет.

### Продукты `/products`

- API, состояние формы и фильтры сохранены в контейнере `ProductsPage.jsx`;
  интерфейс разделён на сводку, редактор, каталог и карточки продуктов.
- Таблица с фиксированной шириной заменена адаптивными карточками. На одной карточке
  видны поставщик, закупочная и складская цена, пересчёт единиц, отход и избранное.
- Редактор объясняет связь закупочной и базовой единицы, автоматически подставляет
  коэффициент и показывает стоимость грамма, миллилитра или штуки до сохранения.
- Добавлены поиск, сортировка и фильтр избранного с сохранением в URL, понятная
  валидация с переводом фокуса, состояния сохранения и загрузка изображения.
- Проверены 1440 / 768 / 390 / 320 px, поиск, избранное, редактирование, пересчёт,
  валидация и загрузка фото. Скриншоты находятся в
  `output/playwright/products-responsive`.
- Production build и `git diff --check` проходят; ошибок в консоли браузера нет.

### Поставщики `/suppliers` и ассортимент `/suppliers/:id`

- Каталог поставщиков разделён на сводку закупок, единый редактор, фильтры,
  карточки партнёров и отдельный диалог удаления; API-состояние осталось в
  контейнере `SuppliersPage.jsx`.
- Все действия переведены на русский. Контакт распознаётся как телефон или почта,
  а ассортимент открывается обычной ссылкой, доступной для новой вкладки.
- Поиск и сортировка сохраняются в URL. Добавлены inline-ошибки, статусы операций,
  пустые состояния и подтверждение удаления с фокус-ловушкой, Escape и возвратом
  фокуса.
- Широкая таблица ассортимента заменена адаптивными карточками; сохранены создание,
  редактирование, фото, избранное, закупочная цена, отход и пересчёт единиц.
- Для уже существовавшего сценария редактирования поставщика добавлен отсутствующий
  backend-контракт `PUT /api/supplier/:id`.
- Проверены 1440 / 768 / 390 / 320 px, поиск, создание, редактирование, удаление,
  фильтр избранного, пересчёт и загрузка фото. Скриншоты находятся в
  `output/playwright/suppliers-responsive`.
- Frontend production build и backend Docker build проходят; ошибок в консоли
  браузера нет.

### Накладные `/consigment` и печать `/consignment-notes/print/:id`

- Контейнер страницы оставляет у себя API, состояние и координацию, а маршрут
  документа, создание, журнал, карточки, позиции и подтверждение удаления
  вынесены в отдельные компоненты.
- Техническая таблица документов заменена адаптивным журналом. Поиск по
  поставщику/номеру и фильтр статуса сохраняются в URL.
- В рабочем окне черновика сохранены добавление и удаление позиций, пересчёт
  суммы и проведение на склад. Ошибки стали inline-сообщениями с переводом
  фокуса; модалка удерживает фокус, закрывается по Escape и возвращает его.
- Дробные количества принимают запятую и точку. Закупочная цена теперь
  сохраняется в `GROSS`, которое backend передаёт в складское движение.
- Backend возвращает `consProductId`, поэтому удаление конкретной строки работает
  стабильно; добавлена серверная валидация количества и цены.
- Печатная форма использует `API_BASE_URL`, показывает фактические цены документа,
  поставщика, склад, подписи и итог, а также отправляет документ в Python-сервис.
- Проверены 1440 / 390 / 320 px, добавление позиции, проведение, просмотр,
  печать, внутренний скролл модалки и отсутствие горизонтального переполнения.
  Скриншоты находятся в `output/playwright/consignment-responsive`.
- Frontend production build и backend Docker build проходят; ошибок и
  предупреждений в консоли браузера на проверенных сценариях нет.

### Склады `/warehouse` и движения `/movements`

- Экран склада разделён на карту хранения, управление точками, маршрут передачи,
  карточки остатков и сменный отчёт. API, состояние и координация остались в
  контейнере, крупная разметка вынесена в 4 компонента и утилиты единиц.
- Восстановлены возможности полной версии страницы: главный склад, приход
  существующего товара, создание нового товара с первым приходом, внутреннее
  перемещение, приход и списание из строки остатка.
- Прямое изменение количества заменено журналируемыми документами `receipt`,
  `movement` и `writeoff`. Проведённые накладные отображаются в том же журнале
  как приход на выбранный склад.
- Остатки и средние цены переводятся из базовых граммов/миллилитров в
  пользовательские кг/л; дробные значения принимают запятую и точку.
- Журнал движений заменил широкую таблицу адаптивной хронологией с маршрутом,
  суммой и сворачиваемым редактированием даты. Поиск, склад, тип и сортировки
  сохраняются в URL.
- Сохранены отчёты по динамике закупок и обороту, выбор товара и печать PDF.
  Сменный складской отчёт приведён к общей визуальной системе.
- Проверены передача товара, URL-фильтр, 1440 / 390 / 320 px, отсутствие
  горизонтального переполнения и ошибок консоли. Скриншоты находятся в
  `output/playwright/warehouse-movements`.
- Frontend production build и Docker build проходят. Предупреждения относятся
  только к старым экранам ML/кассы.

### Клиенты `/clients` и персонал `/person`

- Клиенты собраны как гостевая книга: сводка, создание, URL-поиск, карточки,
  долговая лента и профиль с любимыми блюдами и хронологией заказов.
- Сохранены локально добавленные выдача одноразового VK-кода и backend-контракт
  `POST /api/clients/:id/vk-link-code`; код и срок действия встроены в профиль.
- Состав заказа загружается по требованию. Сохранены закрытие одного или всех
  долгов с подтверждением и обновлением сводки.
- Персонал собран как сменная доска: регистрация аккаунта, URL-поиск, отработанные
  дни, начисления, история локальных выплат и архивирование сотрудника.
- Убраны прямые адреса `http://localhost:8080`; обе страницы и форма регистрации
  используют общий `API_BASE_URL`. Независимые запросы выполняются параллельно.
- Широкая таблица персонала заменена адаптивными карточками. Формы получили
  labels, names, autocomplete, корректные типы полей и inline-ошибки.
- Проверены гостевой профиль, VK-код, состав заказа, долговая лента, URL-поиск,
  1440 / 768 / 390 / 320 px и длинные имена. Горизонтального переполнения и ошибок
  консоли нет. Скриншоты находятся в `output/playwright/clients-person`.
- Production build и Docker build проходят. Предупреждения относятся только к
  старым экранам ML/кассы.

### Касса `/cashier` и экран кухни `/kitchen-display/:shiftId`

- Касса собрана как сервисная линия: открытие и восстановление смены, команда,
  конструктор чека, живая очередь, долги, оплата, готовность, выдача, Z-отчёт,
  печать и инвентаризация перед закрытием сохранены.
- Монолитная разметка страницы разделена на header, сменный журнал, конструктор,
  очередь, карточку заказа и доступные диалоги. API и координация остались в
  контейнере.
- Карточка заказа получила устойчивый таймер, понятные состояния задержки,
  оплаты и долга, компактный состав и действия без встроенных CSS-стилей.
- Каталог позиций принимает оба варианта идентификатора `dishId` / `dishID`,
  удерживает фокус, закрывается по Escape и возвращает фокус на вызвавшую кнопку.
- Экран кухни оформлен как высококонтрастное табло со счётчиками, часами,
  автоперелистыванием и выделением только что готовых заказов. На узком экране
  обе линии доступны обычной вертикальной прокруткой.
- Проверены закрытая и открытая смены, поиск гостя, доставка, каталог, долги,
  1440 / 390 / 320 px и отсутствие горизонтального переполнения. Скриншоты находятся
  в `output/playwright/cashier-kitchen`.
- Production build проходит без предупреждений в кассе и кухне; Docker-образ
  frontend пересобран. Оставшиеся ESLint warnings относятся только к ML.

### AI-аналитика `/ml`

- Раздел собран как единый пульт решений: прогноз спроса, оптимизация состава,
  аналитика качества и генерация нового блюда используют общую иерархию,
  форматирование и состояния.
- Вкладки и период аналитики сохраняются в URL (`view`, `period`). На мобильном
  активная вкладка автоматически попадает в видимую область.
- Прогноз получил устойчивую двухколоночную компоновку, доступный выбор
  ингредиентов, понятные пустое/ошибочное/загрузочное состояния и единый вывод
  уверенности, спроса и экономики.
- Оптимизатор больше не дублирует разметку для вариантов ответа API. Популярные
  ингредиенты и стартовый результат загружаются параллельно, время генерации
  фиксируется, а кеш сохраняет параметры и результат.
- Аналитика ходит через защищённый Java endpoint `/api/analytics/dashboard`,
  а не напрямую в Python на `:8000`. При недоступности сервиса интерфейс явно
  помечает демонстрационные данные.
- Удалена кнопка синхронизации с несуществующим `/api/ml/sync-latest`.
  Обучение использует общий `API_BASE_URL` и показывает корректный статус.
- Формы получили labels, names, autocomplete, `Intl`-форматирование и доступные
  async-сообщения. Устранены предупреждения React Hooks, прямые localhost URL,
  `alert`, `transition: all` и вложенный `<main>`.
- Проверены прогноз, оптимизация, аналитика, генератор, URL-вкладки, 1440 / 390 /
  320 px, отсутствие горизонтального переполнения и ошибок консоли. Скриншоты
  находятся в `output/playwright/ml`.
- Production build проходит без ESLint warnings; Docker-образ frontend
  пересобран.

### Налог `/tax`

- Старый синий admin-dashboard заменён на рабочий пульт CafeHelp с явным
  маршрутом «оплаченные заказы → Outbox → налоговая БД → канал партнёра».
- Сохранены все операции: одиночная отправка, полный запуск, backfill, relay,
  отправка партнёру, повтор ошибок, фильтры периода, лимиты и автообновление.
- Кликабельные строки заменены семантическими кнопками заказа; статусы, даты,
  суммы и async-сообщения получили единое форматирование и доступные состояния.
- На ширине до 760 px широкие таблицы заменяются карточками без горизонтального
  скролла. Диалог трассировки удерживает фокус, закрывается по Escape, блокирует
  прокрутку фона и возвращает фокус на кнопку заказа.
- Проверены 1440 / 390 px, мобильные журналы, диалог, консоль и отсутствие
  горизонтального переполнения. Скриншоты находятся в
  `output/playwright/final-audit`.
- В движениях склада системные `alert` для дат и периодов заменены встроенными
  сообщениями с `role="alert"`.
- Production build проходит без ESLint warnings; Docker-образ frontend
  пересобран и контейнер запущен.

## Текущая итерация

### Общий связный проход

1. Проверить и заменить оставшиеся системные `alert` в кассе и карточках блюд.
2. Унифицировать подтверждения удаления через общий доступный диалог.
3. Провести финальный связный проход по навигации между разделами.
4. Подготовить тематические коммиты без несвязанных файлов рабочего дерева.

## Очередь страниц

1. Финальная унификация сообщений и подтверждений.
2. Связный регрессионный проход по навигации.

## Известный технический долг

- Проект использует старый `react-scripts@5`; `npm test` конфликтует с текущим
  разрешением `react-router-dom@7`, хотя production build проходит.
- В проекте есть существующие ESLint warnings вне уже обновлённых экранов.
- `npm ci` сообщает о 56 уязвимостях зависимостей, включая 2 critical; автоматический
  `npm audit fix` не запускался, чтобы не вносить ломающие обновления.
- В маршрутах есть историческое написание `/consigment`; не менять без отдельной
  проверки ссылок и backend-контрактов.

## Проверка каждой страницы

- Основной сценарий работает с реальным или замоканным API.
- Нет горизонтального скролла на 320 px.
- Все действия доступны с клавиатуры и имеют видимый focus.
- Модальные окна удерживаются в viewport и прокручиваются внутри.
- Ошибки и пустые состояния объясняют следующий шаг.
- `npm run build` и `git diff --check` проходят.
